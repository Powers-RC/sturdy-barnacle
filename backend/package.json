{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "heroku-postbuild": "concurrently --kill-others-on-fail \"npm run start:db\" \"npm run build --prefix ..\"",
    "start:db": "sh -ac '. .env.$REACT_APP_ENV; nodemon ./bin/www'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Cameron Powers",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.3",
    "dotenv": "^6.1.0",
    "express": "^4.16.4",
    "morgan": "^1.9.1",
    "pg": "^7.6.0",
    "pg-hstore": "^2.3.2"
  },
  "devDependencies": {
    "nodemon": "^1.18.6"
  }
}
